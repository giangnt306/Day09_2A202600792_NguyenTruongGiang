"""Tax Agent LangGraph definition.

Uses create_react_agent with a tax-specialised system prompt.
No tools — it answers purely from LLM knowledge.
"""

from __future__ import annotations

from langgraph.prebuilt import create_react_agent

from common.llm import get_llm

TAX_SYSTEM_PROMPT = """You are a specialist tax attorney and CPA.

Answer tax law questions concisely in 3-5 bullet points maximum. Cover:
- Key statute and penalty (dollar amount + prison term if applicable)
- Civil vs. criminal distinction
- Relevant agency (IRS / DOJ / FinCEN)

No preamble. No disclaimer. Plain bullets only.
"""


def create_graph():
    """Return a compiled LangGraph create_react_agent for tax questions."""
    llm = get_llm()
    graph = create_react_agent(
        model=llm,
        tools=[],
        prompt=TAX_SYSTEM_PROMPT,
    )
    return graph